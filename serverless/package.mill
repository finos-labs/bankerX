
package build.serverless
import build.V 
import mill._, scalalib._

object `package` extends RootModule with build.BankerXScalaModule {
  //def moduleDeps = Seq(build.bar.qux.mymodule)
  def ivyDeps = Agg(
    ivy"com.softwaremill.sttp.tapir::tapir-aws-cdk::${V.Libs.tapir}",
    ivy"com.github.alexarchambault::case-app-cats::${V.Libs.`case-app`}",
    ivy"org.typelevel::cats-effect::${V.Libs.`cats-effect`}",
  )
}